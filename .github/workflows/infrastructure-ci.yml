name: Infrastructure CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      request_id:
        description: bootstrap publisher request id
        required: true
        type: string
      new_head:
        description: exact task-branch commit SHA to validate
        required: true
        type: string

permissions:
  contents: read

jobs:
  bootstrap:
    name: bootstrap
    runs-on: ubuntu-latest
    steps:
      - name: Verify dispatched source SHA
        if: github.event_name == 'workflow_dispatch'
        env:
          EXPECTED_HEAD: ${{ inputs.new_head }}
        run: |
          case "$EXPECTED_HEAD" in
            [0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]) ;;
            *) echo "invalid dispatched new_head" >&2; exit 1 ;;
          esac
          test "$GITHUB_SHA" = "$EXPECTED_HEAD"
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.new_head || github.sha }}
          persist-credentials: false
      - name: Verify exact checked out HEAD
        if: github.event_name == 'workflow_dispatch'
        env:
          EXPECTED_HEAD: ${{ inputs.new_head }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD"
      - name: Install contract validator
        run: python -m pip install --disable-pip-version-check 'jsonschema==4.23.0'
      - name: Show imported baseline SHA-256
        run: sha256sum baseline/I00_BASELINE.json
      - name: Validate I01 bootstrap invariants
        run: python control/validate_bootstrap.py .
      - name: Run bootstrap and contract tests
        run: python -m unittest discover -s tests -p 'test_*.py' -v
